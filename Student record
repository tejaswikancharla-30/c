#include <stdio.h>

struct Student {
    int roll;
    float marks;
};

int main() {
    struct Student s = {101, 95.5};

    printf("Roll = %d\n", s.roll);
    printf("Marks = %.1f", s.marks);

    return 0;
}
