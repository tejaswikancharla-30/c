include <stdio.h>

int main() {
    int a[5] = {12, 45, 7, 89, 23};
    int i, max = a[0];

    for(i = 1; i < 5; i++) {
        if(a[i] > max)
            max = a[i];
    }

    printf("Largest element = %d", max);

    return 0;
}
