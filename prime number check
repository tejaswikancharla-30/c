#include <stdio.h>

int main() {
    int n = 7, i, flag = 1;

    for (i = 2; i < n; i++) {
        if (n % i == 0) {
            flag = 0;
            break;
        }
    }

    if (flag == 1)
        printf("%d is Prime", n);
    else
        printf("%d is Not Prime", n);

    return 0;
}
